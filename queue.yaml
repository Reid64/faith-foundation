# faith-foundation — FORGE 2.0 Build Queue
# Generated by the FORGE Queue Generator (s4-p02) on 2026-06-12T07:20:18.452Z.
# Working directory: C:\Users\suppo\Documents\faith-foundation
# Each prompt is executed via: claude -p --dangerously-skip-permissions
#
# 2 prompt(s), 0 parallel group(s), longest dependency chain 2, ~8000 estimated tokens.
# Standard build order: schema → auth → api → ui → features → agents → dashboards → settings → tests → deploy → verify.
# Build HALTS after governance + queue for Gate 3 (human approval, Contract 2).

# ─────────────────────────────────────────────────────────────────────────────
# COMPLETED OUT OF QUEUE — Phase 24: fund designation through the ledger (2026-08-18)
#
# A comment, not an entry, for the same reason as the phases below.
#
#   Added:    supabase/migrations/015_fund_designation_through_ledger.sql
#             src/lib/faithproof/funds.ts          (one shared campaign->fund map)
#             scripts/fund-designation.spec.ts     (9 tests)
#             scripts/zeffy-inspect.mjs            (read-only IMAP inspector)
#   Touched:  src/lib/faithproof/types.ts          (DONOR_FUNDS, ZEFFY_FUND_LABELS,
#                                                   labels matched to the live form)
#             src/lib/faithproof/public.ts         (getFundTotals — aggregates only)
#             src/app/admin/transactions/page.tsx  (fund filter, unverified flag)
#             src/app/admin/transactions/[id]/page.tsx
#             src/app/admin/transactions/[id]/edit/actions.ts
#             src/app/admin/accounting/reports/page.tsx  (per-fund breakout x4)
#             src/app/faithproof/page.tsx, .../explorer/page.tsx
#             src/app/api/webhooks/zeffy/route.ts  (uses the shared fund map)
#   Deps:     none.
#   Schema:   migration 015, APPLIED to production. Revenue accounts 4210/4220/4230,
#             journal_lines.fund, transactions.fund_backfilled, revenue_code_for()
#             and all three auto-posting triggers updated.
#
#   GROUND TRUTH CORRECTED: Zeffy offers SIX funds, not ten. Ten was the enum
#   size. The enum keeps ten on purpose — 3 retired programmes with live rows,
#   plus `operational` which is internal and never donor-facing.
#
#   FASB ANSWER: designated gifts were ALREADY posting to restricted net assets.
#   The defect was granularity — Recovery, Reentry and Cornerstone shared revenue
#   account 4600 and could not be told apart. Not a misclassification.
#
#   NOT DONE ON PURPOSE:
#             - /api/cron/poll-donations. The ZOHO_IMAP_* values are encrypted in
#               Vercel and `vercel env pull` returns them empty, so no real Zeffy
#               email could be read. The brief said do not guess the format, and
#               a silently-failing donation parser drops real money.
#               scripts/zeffy-inspect.mjs unblocks it in one command.
#             - the Vercel cron entry and CRON_SECRET (not set; see OPERATOR_ACTIONS §12)
#             - a "Check for Donations" button: none exists anywhere in the admin
#   Verified: tsc 0 errors, build clean, 267 passed / 3 skipped / 0 failed of 270.
#   NOT DEPLOYED. Branch phase-24/fund-designation.

# ─────────────────────────────────────────────────────────────────────────────
# COMPLETED OUT OF QUEUE — Phase 23: opt-in TOTP MFA + full system audit (2026-08-17)
#
# A comment, not an entry, for the same reason as the phases below: every real
# entry here is a prompt a FORGE runner EXECUTES, and a finished phase listed as
# one would be built again.
#
#   Added:    src/app/admin/settings/MfaSection.tsx
#             scripts/mfa.spec.ts
#             scripts/_fixtures.ts            (shared audit fixtures, not a spec)
#             scripts/admin-surface.spec.ts
#             scripts/public-surface.spec.ts
#             scripts/api-authorization.spec.ts
#             scripts/admin-actions.spec.ts
#             governance/AUDIT_REPORT.md
#   Touched:  src/app/admin/settings/page.tsx            (MFA card)
#             src/app/admin/crm/contacts/[id]/page.tsx   (h2 -> h1, no h1 existed)
#             .../meetings/[id]/minutes/MinutesClient.tsx (two missing labels)
#             scripts/admin-navigation.spec.ts           (crawl budget + pattern fixes)
#             scripts/meeting-room.spec.ts               (test email collisions)
#   Regenerated from the live database:
#             governance/BLUEPRINT.md, governance/SCHEMA_REGISTRY.md
#   Deps:     none.
#   Schema:   NONE. Supabase Auth stores MFA factors itself; no migration.
#   NOT DONE ON PURPOSE:
#             - MFA enforcement (needs middleware; runbook in OPERATOR_ACTIONS §11)
#             - authentication on /api/webhooks/zeffy and email-inbound
#               (breaks live Zapier/Zoho integrations — operator decision)
#             - the Zeffy IMAP poller, which does not exist and was not built
#   Verified: tsc 0 errors, build clean, 258 passed / 3 skipped / 0 failed of 261.
#   NOT DEPLOYED. Branch phase-23/mfa-and-system-audit.

# ─────────────────────────────────────────────────────────────────────────────
# COMPLETED OUT OF QUEUE — Phase 21: WebRTC meeting room (2026-08-16)
#
# A comment, not an entry, for the same reason as Phase 20 below: every real
# entry here is a prompt a FORGE runner EXECUTES, and a finished phase listed as
# one would be built again.
#
#   Added:    src/lib/faithproof/pusherServer.ts
#             src/app/api/pusher/auth/route.ts
#             src/app/api/pusher/signal/route.ts
#             src/app/api/webrtc/turn-credentials/route.ts
#             src/app/admin/board/meetings/[id]/room/useMeshCall.ts
#             src/app/admin/board/meetings/[id]/room/useActiveSpeaker.ts
#             scripts/meeting-room.spec.ts
#   Replaced: src/app/admin/board/meetings/[id]/room/MeetingRoom.tsx  (Jitsi -> WebRTC)
#             src/app/admin/board/meetings/[id]/room/page.tsx
#   Touched:  playwright.config.ts (synthetic camera for the room spec)
#             src/lib/faithproof/board.ts, src/app/admin/board/actions.ts,
#             room/actions.ts  (stale Jitsi comments corrected)
#   Deps:     +pusher +pusher-js. Jitsi was a runtime script tag, not a package.
#   Schema:   none.
#   Verified: tsc 0 errors; build clean; route security 12/12;
#             meeting-room.spec.ts 6/6; no server secret in .next/static.
#   NOT verified: any peer connection — needs two browsers on real credentials.
#   Status:   BUILT AND VERIFIED LOCALLY — NOT DEPLOYED.
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# COMPLETED OUT OF QUEUE — Phase 20: Cloudflare Turnstile (2026-08-16)
#
# Recorded as a comment ON PURPOSE. Every entry in this file is a prompt a
# FORGE runner will EXECUTE; adding a finished phase as a real entry would
# schedule it to be built a second time. The queue is a work list, not a log.
#
#   Added:    src/components/TurnstileWidget.tsx
#             src/lib/turnstile.ts
#             src/lib/formSubjects.ts
#             src/app/api/forms/submit/route.ts
#             scripts/turnstile.spec.ts
#   Changed:  src/components/SiteFooter.tsx
#             src/app/contact/ContactForm.tsx
#             src/app/volunteer/VolunteerForm.tsx
#             src/app/apply/ApplicationForm.tsx
#             src/app/faithproof/ImpactReceiptForm.tsx
#             src/lib/web3forms.ts
#   Verified: tsc 0 errors; build clean;
#             turnstile.spec.ts 10/10 across pass and fail modes;
#             site-audit.spec.ts 139 passed / 1 flaky / 0 failed (local build);
#             ad-grants-readiness.spec.ts 62 passed / 2 skipped / 0 failed.
#   Status:   BUILT AND VERIFIED LOCALLY — NOT DEPLOYED. Reid runs deploy.ps1.
#
# The `deploy` entry below still applies and will pick this change up.
# ─────────────────────────────────────────────────────────────────────────────

- id: deploy
  name: "Deploy to production"
  prompt_type: deploy
  dependencies: []
  governance_refs: [BLUEPRINT.md]
  estimated_tokens: 3000
  context_injection:
    schema_sections: []
    behavioral_sections: []
    interaction_maps: []
  description: |
    Deploy the application following the FORGE deployment protocol: tsc --noEmit → build → deploy → tests pass 10/10. Abort the deploy if any gate fails — never force-push broken code.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.

- id: verify-six-laws
  name: "Six Laws verification"
  prompt_type: test
  dependencies: [deploy]
  governance_refs: [TESTING.md, BEHAVIORAL_CONTRACTS.md, SCHEMA_REGISTRY.md]
  estimated_tokens: 5000
  context_injection:
    schema_sections: []
    behavioral_sections: ["API Contracts", "Authentication & Authorization"]
    interaction_maps: []
  description: |
    Run automated Six Laws verification (Contract 19) for the 0 table(s), 0 route(s), and 0 page(s): Law 1 SCHEMA — every table from SCHEMA_REGISTRY.md exists with correct columns and RLS; Law 2 API — every endpoint responds with the contracted status and shape; Law 3 UI — every page renders with no placeholder/"coming soon" text; Law 4 DATA — pages make real API calls to real tables (no mock data); Law 5 WIRING — navigation, forms, and role gates work end-to-end. Laws 1-5 must ALL pass; Law 6 (human verification in the browser) is the final Gate.

    Update STATE_OF_THE_BUILD.md and SESSION_STATE.md from actual codebase audit.
