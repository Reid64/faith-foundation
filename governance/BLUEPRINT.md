# faith-foundation — BLUEPRINT

> Generated by FORGE 2.0 Phase 2 (Governance Generator) on 2026-06-12T07:20:18.402Z.
> GOVERNANCE DOCUMENT — read-only during Phase 3 execution (BEHAVIORAL_CONTRACTS Contract 3).

## Phase 21 — Board meeting room on native WebRTC (added 2026-08-16)

Jitsi removed. The room is now mesh peer-to-peer, so the participant grid is
ours to draw.

```
signalling:  browser ──► /api/pusher/signal ──► Pusher private-meeting-<id> ──► peers
             (server stamps the sender's identity; no client events)
subscribe:   Pusher ──► /api/pusher/auth ──► session + role + RLS on the meeting
media:       browser ◄──── RTCPeerConnection mesh (≤6) ────► browser
relay:       browser ──► /api/webrtc/turn-credentials ──► Cloudflare (token stays server-side)
```

**Added** — `src/lib/faithproof/pusherServer.ts`, `src/app/api/pusher/auth/route.ts`,
`src/app/api/pusher/signal/route.ts`, `src/app/api/webrtc/turn-credentials/route.ts`,
`room/useMeshCall.ts`, `room/useActiveSpeaker.ts`, `scripts/meeting-room.spec.ts`.

**Replaced** — `room/MeetingRoom.tsx`, `room/page.tsx`.

**Dependencies** — `pusher`, `pusher-js` added. Jitsi was never an npm package;
it was a runtime script tag from meet.jit.si, now gone.

**Not touched** — `room/actions.ts` behaviour, and everything under
`meetings/[id]/minutes/**`, the AI minutes route, the signature workflow and the
certified PDF generator, exactly as instructed.

**Schema** — none. `board_meetings.jitsi_room_name` survives, unread.

**Verification** — tsc 0 errors; build clean; route security 12/12; meeting-room
spec 6/6; no server secret appears in `.next/static`. A peer connection is NOT
verified and cannot be from one browser. Not deployed.

---

## Phase 20 — Public form spam protection (added 2026-08-16)

Cloudflare Turnstile now guards every public form. The architectural change is
that submissions no longer go from the browser straight to a third party:

```
before:  browser ──────────────────────────────► formsubmit.co
after:   browser ──► /api/forms/submit ──► siteverify ──► formsubmit.co
                     (rejects with 400 before anything is forwarded)
```

**Added**
- `src/components/TurnstileWidget.tsx` — the one widget; loads the Cloudflare
  script once for the whole site, exposes `onVerify`, resets imperatively.
- `src/lib/turnstile.ts` — server-only siteverify. Fails closed. Never imported
  by a client component; `TURNSTILE_SECRET_KEY` carries no `NEXT_PUBLIC_` prefix.
- `src/lib/formSubjects.ts` — the single source for form subjects, read by both
  the forms and the route's allowlist.
- `src/app/api/forms/submit/route.ts` — the gate.
- `scripts/turnstile.spec.ts` — 10 tests.

**Changed** — the five public form components, and `src/lib/web3forms.ts`.

**Not touched** — everything under `/admin/**` (auth-protected), `/login`,
`/faithproof/explorer` (a GET filter bar), and the IntakeChat endpoint.

**Verification:** tsc 0 errors; build clean; turnstile spec 10/10 across pass
and fail modes; the existing site-audit 139 passed / 1 flaky / 0 failed against
a local build with Turnstile in the path. Not deployed — deployment is Reid's
step.

---

> **NOTE ON THE SECTIONS BELOW (2026-08-16).** Everything from "System Identity"
> onward is the original FORGE 2.0 scaffolding from 2026-06-12 and its counts
> are stale by a wide margin — it says 0 tables, 0 API routes and 0 pages, while
> the live system has 14 applied migrations, ~30 tables and 72 built routes. The
> living record of what exists is `STATE_OF_THE_BUILD.md`. Left in place rather
> than rewritten from inference: correcting it properly is a re-generation, not
> an edit, and inventing plausible numbers here would be worse than leaving the
> staleness visible and labelled.

## System Identity
- **Name:** faith-foundation
- **Generated:** 2026-06-12T07:20:18.402Z
- **Build mode:** greenfield

## System Overview
`faith-foundation` is an application designed by FORGE 2.0 from an approved architecture. It comprises 0 data table(s), 0 API route(s), 0 page(s).

## Technology Stack
- **Framework:** nextjs
- **Language:** typescript
- **Database:** supabase
- **Deployment:** vercel
- **Package Manager:** pnpm
- **CLI tools:** git, node, pnpm, supabase, vercel

## Architecture Overview
- **Tables:** 0 (tenant-scoped: none)
- **API routes:** 0
- **Pages:** 0; **components:** 0; **layouts:** 0
- **Interaction maps:** 0 (Contract 18 granularity)
- **Roles:** —
- **Agents:** —

## Project Structure
```
faith-foundation/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
├── components/
├── lib/
│   └── supabase/
│       ├── client.ts
│       └── server.ts
├── supabase/
│   └── migrations/
├── tests/
├── middleware.ts
├── package.json
└── tsconfig.json
```

## Environment Variables
No project-specific environment variables were specified by the architecture.
The FORGE default stack requires at least:
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
```

## Canonical Rules
- 1. Every multi-tenant table carries a company/tenant scope column with RLS enabled (Six Laws Law 1).
- 2. API routes derive `company_id` from the session — NEVER from the request body (Six Laws Law 2).
- 3. On ANY role-fetch failure, middleware redirects to `/login` ONLY — never a default/wrong-role page (Iron Law 4).
- 4. No mocks or placeholder data in production code — all data comes from real tables (Iron Law 8).
- 5. Dashboard HTML is served via no-cache API routes, never directly from `public/` (Iron Law 5).
- 6. `pnpm tsc --noEmit` must return zero errors before any commit (Iron Law 6).
- 7. Governance documents are read-only during Phase 3 execution (Contract 3).
